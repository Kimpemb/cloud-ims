services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: cloudims
      POSTGRES_USER: cloudims
      POSTGRES_PASSWORD: cloudims
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: