spring:
  application:
    name: cloud-ims
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

server:
  port: ${PORT:8080}

aws:
  region: ${AWS_REGION:eu-north-1}
  s3:
    bucket-name: ${S3_BUCKET_NAME}